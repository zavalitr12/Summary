mixin logo(img)
    -
        var mod = {
            
        }
        var mod = pugHelper(attributes.mod);
        var mix = pugHelper(attributes.mix);
    div(class="logo")
        div(class="logo__ring")
        if(img)
            img(class="logo__img" src=`./${img}.png`)